# Firmware Artifacts

[简体中文](firmware_ZH.md)

`firmware/` contains the immutable factory recovery image. It is not source-built
by example CI and is not a replacement for a source-built example artifact.
Example CI may package its own selected build outputs; those diagnostics remain
separate from the released factory image. Source and build instructions for any
future firmware project are not included in this repository yet.
